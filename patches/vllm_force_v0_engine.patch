diff --git a/tmx_jax/scripts/bootstrap_vllm_worker.sh b/tmx_jax/scripts/bootstrap_vllm_worker.sh
--- a/tmx_jax/scripts/bootstrap_vllm_worker.sh
+++ b/tmx_jax/scripts/bootstrap_vllm_worker.sh
@@ -18,6 +18,7 @@ VLLM_TPU_BUCKET_PADDING_GAP="${VLLM_TPU_BUCKET_PADDING_GAP:-}"
 VLLM_TPU_MOST_MODEL_LEN="${VLLM_TPU_MOST_MODEL_LEN:-}"
 VLLM_XLA_CACHE_PATH="${VLLM_XLA_CACHE_PATH:-$HOME/.cache/vllm/xla_cache}"
 DISABLE_LOG_REQUESTS="${DISABLE_LOG_REQUESTS:-1}"
+VLLM_USE_V1="${VLLM_USE_V1:-0}"
 if [ -z "${TPU_VISIBLE_CHIPS:-}" ]; then
   if [ "$TP" = "1" ]; then
     TPU_VISIBLE_CHIPS="0"
@@ -91,6 +92,7 @@ export TPU_PROCESS_BOUNDS="$TPU_PROCESS_BOUNDS"
 export TPU_CHIPS_PER_PROCESS_BOUNDS="$TPU_CHIPS_PER_PROCESS_BOUNDS"
 export TPU_CHIPS_PER_HOST_BOUNDS="$TPU_CHIPS_PER_HOST_BOUNDS"
 export TPU_HOST_BOUNDS="$TPU_HOST_BOUNDS"
+export VLLM_USE_V1="$VLLM_USE_V1"
 export TPU_WORKER_HOSTNAMES="\${TPU_WORKER_HOSTNAMES:-localhost}"
 export VLLM_XLA_CACHE_PATH="\${VLLM_XLA_CACHE_PATH:-$VLLM_XLA_CACHE_PATH}"
 export XLA_PERSISTENT_CACHE_PATH="\${XLA_PERSISTENT_CACHE_PATH:-\$VLLM_XLA_CACHE_PATH}"
diff --git a/tmx_jax/scripts/reload_vllm_worker_detached.sh b/tmx_jax/scripts/reload_vllm_worker_detached.sh
--- a/tmx_jax/scripts/reload_vllm_worker_detached.sh
+++ b/tmx_jax/scripts/reload_vllm_worker_detached.sh
@@ -23,6 +23,7 @@ VLLM_TPU_MOST_MODEL_LEN="${VLLM_TPU_MOST_MODEL_LEN:-}"
 DISABLE_LOG_REQUESTS="${DISABLE_LOG_REQUESTS:-1}"
 LOG_FILE="${LOG_FILE:-$HOME/vllm_reload_worker.log}"
 MODEL_IMPL_TYPE="${MODEL_IMPL_TYPE:-auto}"
+VLLM_USE_V1="${VLLM_USE_V1:-0}"
 if [ -z "${TPU_VISIBLE_CHIPS:-}" ]; then
   if [ "$TP_SIZE" = "1" ]; then
     TPU_VISIBLE_CHIPS="0"
@@ -132,6 +133,7 @@ export TPU_PROCESS_BOUNDS="$TPU_PROCESS_BOUNDS"
 export TPU_CHIPS_PER_PROCESS_BOUNDS="$TPU_CHIPS_PER_PROCESS_BOUNDS"
 export TPU_CHIPS_PER_HOST_BOUNDS="$TPU_CHIPS_PER_HOST_BOUNDS"
 export TPU_HOST_BOUNDS="$TPU_HOST_BOUNDS"
+export VLLM_USE_V1="$VLLM_USE_V1"
 export TPU_WORKER_HOSTNAMES="\${TPU_WORKER_HOSTNAMES:-localhost}"
 export MODEL_IMPL_TYPE="$MODEL_IMPL_TYPE"
 export VLLM_XLA_CACHE_PATH="\${VLLM_XLA_CACHE_PATH:-$VLLM_XLA_CACHE_PATH}"
diff --git a/tmx_jax/scripts/bootstrap_vllm_slice.sh b/tmx_jax/scripts/bootstrap_vllm_slice.sh
--- a/tmx_jax/scripts/bootstrap_vllm_slice.sh
+++ b/tmx_jax/scripts/bootstrap_vllm_slice.sh
@@ -17,6 +17,7 @@ VLLM_TPU_BUCKET_PADDING_GAP="${VLLM_TPU_BUCKET_PADDING_GAP:-}"
 VLLM_TPU_MOST_MODEL_LEN="${VLLM_TPU_MOST_MODEL_LEN:-}"
 VLLM_XLA_CACHE_PATH="${VLLM_XLA_CACHE_PATH:-}"
 DISABLE_LOG_REQUESTS="${DISABLE_LOG_REQUESTS:-1}"
+VLLM_USE_V1="${VLLM_USE_V1:-0}"
 VLLM_ENV_CACHE_GCS_URI="${VLLM_ENV_CACHE_GCS_URI:-gs://two-model-exploration-checkpoints/shared_envs/v5lite_vllm_tpu_0.13.3_py311.tar.gz}"
 LAUNCH_RETRIES="${TMX_VLLM_BOOTSTRAP_RETRIES:-4}"
 LAUNCH_RETRY_SLEEP="${TMX_VLLM_BOOTSTRAP_RETRY_SLEEP:-8}"
@@ -72,6 +73,7 @@ for w in $WORKERS; do
         VLLM_TPU_MOST_MODEL_LEN='$VLLM_TPU_MOST_MODEL_LEN' \
         VLLM_XLA_CACHE_PATH='$VLLM_XLA_CACHE_PATH' \
         DISABLE_LOG_REQUESTS='$DISABLE_LOG_REQUESTS' \
+        VLLM_USE_V1='$VLLM_USE_V1' \
         VLLM_ENV_CACHE_GCS_URI='$VLLM_ENV_CACHE_GCS_URI' \
         bash ~/bootstrap_vllm_worker.sh
       " >/dev/null; then
diff --git a/tmx_jax/scripts/reload_vllm_direct_slice.sh b/tmx_jax/scripts/reload_vllm_direct_slice.sh
--- a/tmx_jax/scripts/reload_vllm_direct_slice.sh
+++ b/tmx_jax/scripts/reload_vllm_direct_slice.sh
@@ -28,6 +28,7 @@ VLLM_TPU_BUCKET_PADDING_GAP="${VLLM_TPU_BUCKET_PADDING_GAP:-}"
 VLLM_TPU_MOST_MODEL_LEN="${VLLM_TPU_MOST_MODEL_LEN:-}"
 VLLM_XLA_CACHE_PATH="${VLLM_XLA_CACHE_PATH:-}"
 DISABLE_LOG_REQUESTS="${DISABLE_LOG_REQUESTS:-1}"
+VLLM_USE_V1="${VLLM_USE_V1:-0}"
 PARALLELISM="${PARALLELISM:-8}"
 LAUNCH_RETRIES="${TMX_VLLM_RELOAD_LAUNCH_RETRIES:-4}"
 LAUNCH_RETRY_SLEEP="${TMX_VLLM_RELOAD_LAUNCH_RETRY_SLEEP:-8}"
@@ -145,6 +146,7 @@ nohup env \\
   VLLM_TPU_MOST_MODEL_LEN='${VLLM_TPU_MOST_MODEL_LEN}' \\
   VLLM_XLA_CACHE_PATH='${VLLM_XLA_CACHE_PATH}' \\
   DISABLE_LOG_REQUESTS='${DISABLE_LOG_REQUESTS}' \\
+  VLLM_USE_V1='${VLLM_USE_V1}' \\
   MODEL_IMPL_TYPE='${MODEL_IMPL_TYPE}' \\
   LOG_FILE='${log_file}' \\
   bash /tmp/reload_vllm_worker_detached.sh > ${log_file}.nohup 2>&1 &
